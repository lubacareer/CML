{
  "name": "case-of-the-missing-logic",
  "description": "A 2D point-and-click comedy mystery game built with Phaser, TypeScript, and Vite.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-hazel-spritesheet.mjs && node scripts/build-hazel-4dir-spritesheet.mjs && node scripts/build-ui-icons.mjs",
    "dev": "vite --config vite/config.dev.mjs",
    "build": "vite build --config vite/config.prod.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "typescript": "~5.7.2",
    "vite": "^6.3.1",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "phaser": "4.0.0",
    "terser": "^5.39.0"
  }
}
